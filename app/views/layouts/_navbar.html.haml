%nav
  %ul.nav.nav-pills
    %li
      .navbar-brand DrawTag
    .navbar-right.nav.nav-pills
      -if user_signed_in?
        %li.active
          = link_to('Logout', destroy_user_session_path, :method => :delete)
      -else
        %li.active
          %a#signin-button{ 'data-toggle' => "modal", 'data-target' => "#signup" } 
            SignUp
        %li
          %a#signin-button{ 'data-toggle' => "modal", 'data-target' => "#signin" } 
            SignIn

- @content = { title: 'Sign In', id: 'signin', location: 'devise/sessions/new' }
= render partial: 'layouts/modal', content: @content
- @content = { title: 'Sign Up', id: 'signup', location: 'devise/registrations/new' }
= render partial: 'layouts/modal', content: @content